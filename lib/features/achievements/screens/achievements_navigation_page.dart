import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';

/// Tab wrapper for the achievements substack (mirrors
/// `surveys_navigation_page.dart`).
@RoutePage()
class AchievementsNavigationPage extends StatelessWidget {
  const AchievementsNavigationPage({super.key});

  @override
  Widget build(BuildContext context) => const AutoRouter();
}
